# Sensor Integration
Simulink models for sensor interfacing and validation (Phase 5).

## Files (to be created)
- `sensor_validation_harness.slx` — Sensor vs MoCap ground-truth comparison
- `optical_flow_interface.slx` — PMW3901 interface model
- `lidar_interface.slx` — VL53L1X interface model
