# Custom Flight Controller
Simulink models for cascaded PID controller with auto-tuning (Phase 4).

## Files (to be created)
- `flight_controller.slx` — Main controller model
- `pid_autotuner_harness.slx` — Auto-tuning test harness
- `gain_schedule_config.m` — Gain scheduling breakpoints and config
