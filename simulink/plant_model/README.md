# Plant Model
Simulink models for QAV250 quadrotor dynamics (Phase 3).

## Files (to be created)
- `qav250_plant.slx` — Main 6-DOF plant model
- `motor_propeller.slx` — Motor/propeller subsystem library
- `sensor_models.slx` — Sensor noise model library
